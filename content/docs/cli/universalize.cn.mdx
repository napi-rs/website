---
description: '@napi-rs/cli 中的 napi universalize 命令。'
---

# 创建通用二进制文件

import { Green } from '../../../components/chalk'

将已构建的二进制文件合并为一个通用二进制文件。

## 用法

```sh
# CLI
napi universalize [--options]
```

```typescript
// Programmatically
import { NapiCli } from '@napi-rs/cli'

new NapiCli().universalize({
  // options
})
```

## 选项

| 选项 | CLI 选项 | 类型 | 必填 | 默认值 | 描述 |
| --- | --- | --- | --- | --- | --- |
| | --help,-h | | | | 获取帮助 |
| cwd | --cwd | string | false | process.cwd() | napi 命令执行的工作目录，其他所有路径选项都相对于该路径 |
| configPath | --config-path,-c | string | false | | <Green>napi</Green> 配置 JSON 文件的路径 |
| packageJsonPath | --package-json-path | string | false | <Green>package.json</Green> | <Green>package.json</Green> 的路径 |
| outputDir | --output-dir,-o | string | false | <Green>./</Green> | 存放所有已构建 <Green>.node</Green> 文件的目录，与 build 命令的 <Green>--output-dir</Green> 相同 |
