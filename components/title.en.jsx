import { NodeJS, Rust } from './lang'

export const TitleEN = () => (
  <p>
    <span style={{ fontSize: '36px', fontWeight: 'bold' }}>NAPI-RS</span> is a
    framework for building pre-compiled <NodeJS /> addons in <Rust />.
  </p>
)
